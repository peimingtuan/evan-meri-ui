// 类型
export enum SwitchState {
    // 选中
    checked = "checked",
    // 未选中 - 未选中
    uncheck = "uncheck",
}

// 大小
export enum SwitchSize {
    // 默认大小
    default = 'default',
    // 小尺寸
    small = 'small',
}
